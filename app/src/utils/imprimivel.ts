/** 
 * Polimorfismo é a capacidade que um objeto tem,
 * de ser referenciado de múltiplas formas.
 * Diferente de classes, é possível implementar várias interfaces no mesmo contexto
*/

export interface Imprimivel {
    paraTexto(): string
}